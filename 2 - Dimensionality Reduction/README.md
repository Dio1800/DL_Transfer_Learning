# Folder '2 - Dimensionality Reduction' 
Has the python code to read one image and plot:

    - the original image
    - the image in grayscale
    - the image with blur
    - the image in black and white
    - the image in black and white inverted
    - the image generate by applying the image in black and white inverted as a mask to image in grayscale